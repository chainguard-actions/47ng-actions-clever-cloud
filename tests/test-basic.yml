name: Test basic deploy invocation
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
      # Fake credentials — the action will fail to authenticate, which is expected
      CLEVER_TOKEN: fake-token
      CLEVER_SECRET: fake-secret
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: deploy
        uses: .
        continue-on-error: true
        with:
          appID: app_00000000-0000-0000-0000-000000000000

      - name: Assert action ran (not a setup/missing-image error)
        env:
          OUTCOME: ${{ steps.deploy.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action should have run (and failed due to fake credentials),
          # not been skipped or errored before even starting.
          if [ "$OUTCOME" = "skipped" ]; then
            echo "FAIL: action was skipped, expected it to run"
            exit 1
          fi
          echo "PASS: action ran (outcome=$OUTCOME, expected failure with fake credentials)"
