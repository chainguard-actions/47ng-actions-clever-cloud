name: Test quiet and timeout inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with quiet=true and timeout (expect failure - no credentials)
        id: action
        uses: .
        with:
          appID: app_00000000-0000-0000-0000-000000000000
          quiet: 'true'
          timeout: '60'
        env:
          CLEVER_TOKEN: ''
          CLEVER_SECRET: ''
        continue-on-error: true

      - name: Assert action failed (quiet and timeout inputs accepted, failed at auth)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail without credentials, but got: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed (quiet and timeout inputs accepted, failed at auth)"
